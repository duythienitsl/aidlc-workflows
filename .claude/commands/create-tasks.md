---
description: Turn an approved PRD into an ordered backlog with acceptance criteria — second step after write-prd
---

Read and follow the skill at `skills/create-tasks/SKILL.md` in this repository.

Requires an approved PRD (typically `docs/<prd-slug>/prd-document.md`). Default outputs: `docs/<prd-slug>/tasks/backlog.md` and `docs/<prd-slug>/tasks/todo.md` (or paths from `.aidlc/project.yaml`). Stay in planning mode — no feature code until the human accepts the backlog.
