---
description: Implement backlog tasks one by one with tests and verification — third step after create-tasks
---

Read and follow the skill at `skills/execute-tasks/SKILL.md` in this repository.

Use the resolved backlog and todo files (defaults under `docs/<prd-slug>/tasks/` per `skills/_reference/project-context.md`, or paths from `.aidlc/project.yaml`) as the source of truth for the next task. Classify the task and load matching **`skills/playbooks/*/SKILL.md`** per `skills/_reference/execution-playbooks.md`. Complete one task (or one agreed slice), verify with the project’s test/build commands, then commit and update the backlog status.
