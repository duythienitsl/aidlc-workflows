---
description: Produce an approved product requirements document — the first step of the AIDLC pipeline
---

Read and follow the skill at `skills/write-prd/SKILL.md` in this repository.

Do not start implementation or task breakdown until the human confirms the PRD. Default output path is `docs/<prd-slug>/prd-document.md` with `docs/<prd-slug>/tasks/` for backlog and todo (see `skills/_reference/project-context.md`) unless the project already standardizes another location.
