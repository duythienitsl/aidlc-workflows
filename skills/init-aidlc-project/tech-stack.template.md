# Tech stack

Fill this in for your repository. Pipeline skills read this file when resolving domain context (see `.aidlc/project.yaml`).

If this repo was initialized with **greenfield** `init-aidlc-project`, use the **Project shape** in `prd-document.md` and the matching playbook under `skills/_reference/` (`bootstrap-backend.md`, `greenfield-frontend.md`, `greenfield-monorepo-turborepo-bun.md`) to pre-fill the sections below.

If this repo is **brownfield** (existing codebase), set `bootstrap.shape: brownfield` in `.aidlc/project.yaml` and fill the sections below from **what is already in the repo** (e.g. root and package `package.json`, workspace files, lockfiles, `README`, CI config)—not from greenfield playbooks.

## Runtime and language

- Language(s):
- Package manager / lockfile:

## Frameworks and libraries

- Web / API:
- Data / persistence:
- UI (if any):

## Testing and quality

- Test runner / framework:
- Lint / format:

## Build and CI

- Build command(s):
- CI entrypoints (if known):

## Conventions agents must follow

- Prefer patterns:
- Avoid:
