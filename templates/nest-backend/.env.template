PORT=3000
API_ROUTE_PREFIX=v1
CORS_ORIGINS=http://localhost:5173
# Host port must match docker-compose.yml postgres ports (left side). If 5432 is busy, follow skills/_reference/docker-postgres-host-port.md and align this URL.
DATABASE_URL=postgres://app:app@localhost:5432/app

# --- Auth (optional) ---
# Add your project's auth env vars here.
# Follow your domain skill at skills/<your-auth>/SKILL.md for required variables.
# Example for JWKS-based JWT validation:
# AUTH_JWKS_URI=
# AUTH_ISSUER=
# AUTH_AUDIENCE=

# TYPEORM_LOGGING=true
# RUN_MIGRATIONS_ON_BOOT=false
# LOG_LEVEL=info
