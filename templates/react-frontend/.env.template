# Environment variables — copy to .env (Vite exposes only VITE_* to the client).
#
# Fill in values for your project's environment.
# Add variables required by your auth provider or backend here.
# Follow your domain skill at skills/<your-auth>/SKILL.md for auth-specific variables.

# Example API base URL:
# VITE_API_BASE_URL=http://localhost:3000/v1
